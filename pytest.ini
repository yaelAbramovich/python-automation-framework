[pytest]
testpaths = tests
addopts = --browser chromium --screenshot only-on-failure --video retain-on-failure --tracing retain-on-failure --html=playwright-report/report.html --self-contained-html
markers =
    smoke: quick smoke-test subset
    regression: full regression subset
    login: login-flow tests
